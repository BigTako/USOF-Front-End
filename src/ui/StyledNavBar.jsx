import styled from 'styled-components';

const StyledNavBar = styled.nav`
  background-color: var(--color-grey-0);
  min-height: 5.5rem;
  padding: 0 var(--main-header-offset);
`;

export default StyledNavBar;
