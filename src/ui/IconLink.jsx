import styled from "styled-components";

const IconLink = styled.a`
  &:link {
    margin: 0 0.5rem;
    font-size: 4rem;
  }

  &:visited,
  &:active,
  &:link {
    color: inherit;
  }
`;

export default IconLink;
